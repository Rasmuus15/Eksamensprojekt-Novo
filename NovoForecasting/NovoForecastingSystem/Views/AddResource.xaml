<Window x:Class="NovoForecastingSystem.Views.AddResource"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NovoForecastingSystem.Views"
        mc:Ignorable="d"
        Title="Add Resources" Height="520" Width="380" Background="White" FontFamily="Segoe UI">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Add Resources" FontSize="24" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,25"/>

        <!-- Phase -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Phase:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <ComboBox Height="40" VerticalContentAlignment="Center" Padding="10,0"/>
        </StackPanel>

        <!-- Role -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Role:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <ComboBox Height="40" VerticalContentAlignment="Center" Padding="10,0"/>
        </StackPanel>

        <!-- Allocation Status -->
        <Border Grid.Row="3" BorderBrush="#E2E8F0" BorderThickness="1.5" CornerRadius="8" Background="#F8FAFC" Margin="0,10,0,25" Padding="15">
            <Grid>
                <TextBlock Text="Allocation Status:" Foreground="#334155" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock Text="0 / 1" Foreground="#16A34A" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Email -->
        <StackPanel Grid.Row="4" Margin="0,0,0,20">
            <TextBlock Text="Email:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <ComboBox Height="40" VerticalContentAlignment="Center" Padding="10,0"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="6" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Add" Foreground="White" Background="#2563EB" BorderThickness="0" Height="44" FontSize="15" FontWeight="SemiBold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="2" Content="Close" Foreground="#334155" Background="#E2E8F0" BorderThickness="0" Height="44" FontSize="15" FontWeight="SemiBold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>
