<Window x:Class="NovoForecastingSystem.Views.CreateProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NovoForecastingSystem.Views"
        mc:Ignorable="d"
        Title="Create Project" Height="540" Width="380" Background="White" FontFamily="Segoe UI">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Create Project" FontSize="24" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,25"/>

        <!-- Project Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Project Name:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <TextBox x:Name="ProjectNameTextBox" Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}" Height="40" VerticalContentAlignment="Center" Padding="10,0"/>
        </StackPanel>

        <!-- Start Date -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Start Date:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <DatePicker x:Name="StartDatePicker" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" Height="40" VerticalContentAlignment="Center" Padding="10,0">
            </DatePicker>
        </StackPanel>

        <!-- Complexity -->
        <StackPanel Grid.Row="4" Margin="0,0,0,20">
            <TextBlock Text="Complexity:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <ComboBox x:Name="ComplexityComboBox" SelectedValue="{Binding Complexity}" Height="40" VerticalContentAlignment="Center" Padding="10,0">
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,0,0,20">
            <TextBlock Text="Project Coordinator:" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,8"/>
            <ComboBox x:Name="ProjectCoordinatorComboBox" ItemsSource="{Binding ProjectCoordinatorList}" SelectedItem="{Binding SelectedProjectCoordinator, Mode=TwoWay}" DisplayMemberPath="Initials" SelectedValuePath="CoordinatorId" Height="40" VerticalContentAlignment="Center" Padding="10,0">
            </ComboBox>
        </StackPanel>


        <!-- Buttons -->
        <Grid Grid.Row="6" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Create Project" Command="{Binding CreateProjectCommand}" CommandParameter="{Binding}" Foreground="White" Background="#2563EB" BorderThickness="0" Height="44" FontSize="15" FontWeight="SemiBold" Cursor="Hand" Grid.ColumnSpan="2" Margin="0,0,14,0" VerticalAlignment="Top" Click="CreateProject_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="2" Content="Cancel" Click="Cancel_Click" Foreground="#334155" Background="#E2E8F0" BorderThickness="0" Height="44" FontSize="15" FontWeight="SemiBold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</Window>
